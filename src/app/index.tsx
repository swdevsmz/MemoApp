import { JSX } from 'react';
import { View, Text } from 'react-native';

const Index = (): JSX.Element => {
    return (
        <View>
            <Text>Memo App</Text>
        </View>
    );
};

export default Index;